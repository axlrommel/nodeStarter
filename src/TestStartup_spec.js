// eslint-disable-next-line no-console
console.log('Starting up tests');

//set up the env for testing
//process.env.DB_URL = "http://localhost:5984";

beforeEach(()=>{
  //anything you do before EVERY test
});
afterEach(()=>{
  //anything you do after EVERY test
});

//delete envs for testing
//delete process.env.DB_URL;